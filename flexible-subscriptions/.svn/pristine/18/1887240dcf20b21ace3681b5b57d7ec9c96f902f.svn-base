<?php
/**
 * Store retry details in the WordPress custom table.
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
} // Exit if accessed directly

class WCS_Retry_Store {

	/**
	 * Get the details of the last retry (if any) recorded for a given order
	 *
	 * @param int $order_id
	 *
	 * @return WCS_Retry|null
	 */
	public function get_last_retry_for_order( $order_id ) {
		// For now, we don't handle any renewals, so just inform, there were none.
		return null;
	}

}
