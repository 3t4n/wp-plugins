jQuery(document).ready(function($){
    var regex = new RegExp(boomvars.prefix,"g");
    jQuery("img").each(function(){
        var src = jQuery(this).attr("src").replace(regex,'')
         jQuery(this).attr("src",src)
    
    });
});