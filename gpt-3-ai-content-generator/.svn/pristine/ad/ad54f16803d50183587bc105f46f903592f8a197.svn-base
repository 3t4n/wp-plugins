<?php
/**
 * sample functions
 *
 * @package  includes
 * @version  0.0.1
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly.
}

/* ------------------------------------------------------------------------------- */

if ( ! function_exists('wpoai_sample_function') ) :
	/**
	 * Sample_functions
	 *
	 * @return string
	 */
	function wpoai_sample_function() {
	
		$output = '';
	
		return apply_filters( 'wpoai_sample_function', $output );
	}
	endif;